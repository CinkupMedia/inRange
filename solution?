inRange(number, start, end) {
    if(typeof end === 'undefined') {
      end = start;
      start = 0;
    };
    if(start > end) {
      let temp = end;
      end = start;
      start = temp;
    };
    const isInRange = (start <= number && number < end);
  }
